restapi {
    default {
        baseurl = {
            qa = "http://dummy.restapiexample.com"
            dev = "http://dummy.restapiexample.com"
        }
    }
}

serenity {
  project.name = "RetoBackend"
  outputDirectory = "target/site/serenity"
  compress.filenames = true
  take.screenshots = "FOR_EACH_ACTION"
  reports = ["single-page-html"]
}
